diff --git a/net/core/fib_rules.c b/net/core/fib_rules.c
index 9307acc9dfc6..7aad08884b1d 100644
--- a/net/core/fib_rules.c
+++ b/net/core/fib_rules.c
@@ -14,6 +14,10 @@
 #include <net/inet_dscp.h>
 #include <net/sock.h>
 #include <net/fib_rules.h>
+#ifdef CONFIG_VPNHIDE
+#include "../../security/vpnhide/vpnhide.h"
+#endif
+
 #include <net/ip_tunnels.h>
 #include <linux/indirect_call_wrapper.h>
 
@@ -1013,6 +1017,12 @@ static int fib_nl_fill_rule(struct sk_buff *skb, struct fib_rule *rule,
 {
 	struct nlmsghdr *nlh;
 	struct fib_rule_hdr *frh;
+#ifdef CONFIG_VPNHIDE
+	if (is_target_uid() &&
+	    ((rule->iifname[0] && vpnhide_should_hide_ifname(rule->iifname)) ||
+	     (rule->oifname[0] && vpnhide_should_hide_ifname(rule->oifname))))
+		return 0;
+#endif
 
 	nlh = nlmsg_put(skb, pid, seq, type, sizeof(*frh), flags);
 	if (nlh == NULL)
