_2026-07-05_

## English

Only clear rolling stats on a real stats-window change, instead of on every settings sync (app toggles, port rules, etc. were resetting native intercept counters every few seconds)

## Русский

Скользящая статистика теперь сбрасывается только при реальном изменении окна периода, а не при каждой синхронизации настроек (переключение приложений, правила портов и т.д. ранее сбрасывали счётчики native-перехватов каждые несколько секунд)
