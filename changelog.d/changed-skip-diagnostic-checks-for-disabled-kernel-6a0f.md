_2026-06-29_

## English

Skip diagnostic checks for disabled kernel hooks: checks that require inactive hooks now show grey 'Skipped' status instead of failing; protection counts on the dashboard exclude skipped checks and never show 'Partial' when all active hooks pass

## Русский

Пропуск диагностических проверок для отключённых хуков ядра: проверки, требующие неактивных хуков, теперь отображаются серым статусом «Пропущено» вместо провала; счётчики защиты на дашборде исключают пропущенные проверки и не показывают «Частично», если все активные хуки проходят
