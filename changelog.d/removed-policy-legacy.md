## Removed

- Removed the legacy per-component policy setter ABI; policy updates now go exclusively through the atomic `VH_SET_POLICY` snapshot contract.
- Удалён legacy ABI поштучной записи policy; обновления выполняются только через атомарный контракт snapshot `VH_SET_POLICY`.
